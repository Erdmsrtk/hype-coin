name: Run Hype Coin Bot

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install python-telegram-bot requests pandas

      - name: Test Telegram connectivity
        run: |
          python - <<'EOF'
import os
from telegram import Bot
bot = Bot(os.getenv("TELEGRAM_TOKEN"))
try:
    r = bot.send_message(chat_id=os.getenv("TELEGRAM_CHAT_ID"), text="📡 [CI] Test mesajı")
    print("✅ Test mesajı gönderildi:", r)
except Exception as e:
    print("❌ Test hatası:", e)
EOF
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.CHAT_ID }}

      - name: Run hype_coin_detector_bot.py
        run: python hype_coin_detector_bot.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.CHAT_ID }}
          VS_CURRENCY: 'usd'
